import styled from 'styled-components';
import cores from '../../styles/cores';

export const BtnCarrinhoStyled = styled.button`
    color: ${cores.salmao};
    font-size: 18px;
    text-decoration: none;
    cursor: pointer;
    background-color: transparent;
    border: none;
`;
